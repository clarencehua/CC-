#include <stdio.h>

int Cube (int a)
{
	int result = a * a * a;  // 计算出结果
	return result;  // 返回结果
}

int main()
{
	int n = Cube ( 12 ); // 调用函数：传入参数，得到返回值
	printf(" result: %d \n", n);

	return 0;
}
