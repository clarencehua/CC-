#include <stdio.h>

void sum (int* a, int* b, int* out)
{
	int result = *a + *b; // 读取输入
	*out = result; // out保存输出, 不使用返回值
}

int main()
{
	int a=10, b=11;
	int out = 0;
	sum(&a, &b, &out);
	return 0;
}
