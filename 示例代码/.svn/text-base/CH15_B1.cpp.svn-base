#include <stdio.h>

int GetLength (const char* p)
{
	int count = 0;
	while( p[count] ) 
		count ++;	
	return count;
}

int main()
{
	int n = GetLength("hello,world");
	printf("size: %d \n", n);
	return 0;
}
