
#include <stdio.h>

extern int abc; // 声明一个全局变量
extern void Print(int); // 声明一个全局函数

int main()
{
	abc = 11;
	Print(abc);
	return 0;
}

int abc = 0; // 全局变量的定义 
void Print(int val)
{
	printf("value: %d \n", val);
}
