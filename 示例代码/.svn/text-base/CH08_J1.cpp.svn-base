#include <stdio.h>

// 求两个double数中的较大值
double find_max(double a, double b)
{
	return a > b ? a : b;
}
// 求两个int数中的较大值
int find_max(int a, int b)
{
	return a > b ? a : b;
}

int main()
{
	double m1 = find_max(1,2);
	return 0;
}


