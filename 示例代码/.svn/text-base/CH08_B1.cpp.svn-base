#include <stdio.h>

double  Average (int a, int b , int c)
{
	double result = (a + b + c) / 3.0;
	return result;
}

int main()
{
	double r = Average(10, 31, 123);
	printf(" result: %.3lf \n", r);
	return 0;
}
