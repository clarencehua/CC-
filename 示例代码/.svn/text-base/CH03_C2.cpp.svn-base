#include <stdio.h>

int main()
{
	char  a = 0x1A;  // 即十进制的26
	int   b = 0x1A2B3C4D;  // 即十进制的439041101
	printf("a : address = %08X \n", &a);  // &a表示变量a的地址
	printf("b : address = %08X \n", &b);  // &b表示变量b的地址

	return 0;
}
