#include <stdio.h>

int avg(int* p, int len)
{
	int sum = 0;
	for(int i=0; i<len; i+)
	{
		sum += p[i];
	}
	return sum/len;
}

int main()
{
	int arr[] = {0, 1, 2, 3};
	int ret ;

	ret = avg(arr, 4); //´Óarr[0]µ½arr[3]
	ret = avg(arr, 3); // arr[0] .. arr[2]
	ret = avg(arr + 1, 3); // arr[1] .. arr[3]
	return 0;
}

